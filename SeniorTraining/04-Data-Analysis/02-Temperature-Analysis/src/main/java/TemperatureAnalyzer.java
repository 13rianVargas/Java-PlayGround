/**
 * EXERCICIO — Temperatura más cercana a cero
 *
 * Dado un array de temperaturas, encuentra la más cercana a 0.
 * Si hay un empate entre un número positivo y su negativo (ej: -5 y +5),
 * el número positivo gana.
 *
 * Si el array está vacío, retorna 0.
 *
 * Este es un ejercicio CodinGame clásico que aparece en pruebas reales de
 * selección.
 */
public class TemperatureAnalyzer {

    /**
     * @param temperatures array de temperaturas (puede ser null o vacío)
     * @return temperatura más cercana a 0; prefiere positiva en empate;
     *         retorna 0 si el array es null o vacío
     */
    public static int closestToZero(int[] temperatures) {
        // TODO: Implementa este método
        throw new UnsupportedOperationException("Implementa este método.");
    }
}
